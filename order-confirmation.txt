'use client'

import { CheckCircle, Home, History } from 'lucide-react'
import Link from 'next/link'

interface OrderConfirmationProps {
  orderId: string
  total: number
  itemCount: number
  onClose: () => void
}

export default function OrderConfirmation({
  orderId,
  total,
  itemCount,
  onClose,
}: OrderConfirmationProps) {
  return (
    <div className="fixed inset-0 bg-black/50 z-50 flex items-center justify-center p-4">
      <div className="bg-white rounded-xl max-w-md w-full shadow-2xl animate-slide-in">
        <div className="bg-gradient-to-r from-green-500 to-emerald-500 p-8 text-center text-white rounded-t-xl">
          <div className="inline-flex items-center justify-center">
            <CheckCircle className="w-16 h-16 animate-bounce" />
          </div>
          <h2 className="text-2xl font-bold mt-4">Payment Successful!</h2>
          <p className="text-green-100 mt-1">Your order has been confirmed</p>
        </div>

        <div className="p-8 space-y-6">
          {/* Order ID */}
          <div className="bg-gray-50 p-4 rounded-lg text-center">
            <p className="text-sm text-gray-600 mb-1">Order ID</p>
            <p className="text-2xl font-bold text-indigo-600 font-mono">
              {orderId}
            </p>
          </div>

          {/* Order Details */}
          <div className="space-y-3">
            <div className="flex justify-between text-gray-700">
              <span>Items:</span>
              <span className="font-semibold">{itemCount}</span>
            </div>
            <div className="flex justify-between text-gray-700">
              <span>Total Amount:</span>
              <span className="font-semibold text-lg text-indigo-600">
                ${total.toFixed(2)}
              </span>
            </div>
            <div className="flex justify-between text-gray-700">
              <span>Status:</span>
              <span className="font-semibold text-green-600">Completed</span>
            </div>
          </div>

          {/* Message */}
          <div className="bg-blue-50 p-4 rounded-lg text-center">
            <p className="text-sm text-blue-700">
              ✓ Your receipt has been sent to your email<br/>
              ✓ Thank you for shopping with RetailVision
            </p>
          </div>

          {/* Actions */}
          <div className="flex gap-3 pt-4">
            <button
              onClick={onClose}
              className="flex-1 btn-primary flex items-center justify-center gap-2"
            >
              <Home className="w-5 h-5" />
              Continue Shopping
            </button>
          </div>
        </div>
      </div>
    </div>
  )
}